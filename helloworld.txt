const int MAX=5;
int Variable;

int func(int i,int PAR){
	if(i >= PAR){
		return(PAR);
	}
	if(i<=PAR/100*1)
		return(func(i*100));
	else
		return(func(i*10));
	return(0);
}

void NoReturnFunc(char k){
    if(k == 'H')
        printf(k);
	else
		printf("not H");
	;
	;
	;
}

void main(){
    int a[10];
    for(i=0;i<MAX;i+1){
        scanf(e);
		f[i] = e;
	}
	i=0;
	do{
		e = f[i];
		printf("233");
		i = i+1;
	}while(i<MAX)
	printf("next period\n");
	for( i =0; i<MAX;i+1){
		NoReturnFunc(f[i]);
	}
	printf("next period\n");
	printf(func(100,INTMAX)*'a');
	printf("next period\n");
	b=ASSIGNB;
	c[5] = 7 ;
	if(b>c[5])
		printf("b is bigger than c[5]\n");
	else
		printf("b is not bigger than c[5]\n");
	printf((1+2*3)*111);
}